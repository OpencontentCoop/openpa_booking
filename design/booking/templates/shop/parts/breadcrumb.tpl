<section class="hgroup noborder">
    <ul class="nav nav-pills">
        <li>
            <span class="btn btn-{if $selected|eq(1)}primary{else}default{/if}">
                1. {"Shopping basket"|i18n("design/ocbootstrap/shop/basket")}
            </span>
        </li>
        <li>
            <span class="btn btn-{if $selected|eq(2)}primary{else}default{/if}">
                2. {"Account information"|i18n("design/ocbootstrap/shop/basket")}
            </span>
        </li>
        <li>
            <span class="btn btn-{if $selected|eq(3)}primary{else}default{/if}">
                3. {"Confirm order"|i18n("design/ocbootstrap/shop/basket")}
            </span>
        </li>
    </ul>

</section>
